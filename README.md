# Comentarios persistentes (servidor local)

Este proyecto añade un servidor Node.js mínimo para almacenar comentarios en `comments.txt` y servir tu sitio estático.

Instrucciones:

1. Instala dependencias:

```bash
npm install
# Comentarios (pendiente)

He retirado la integración con Utterances y la configuración del servidor local. La implementación de comentarios se hará más adelante usando una cuenta de Google (Google Forms / Sheets o Apps Script) para que los visitantes puedan comentar sin necesitar cuenta de GitHub.

Pasos futuros recomendados:

- Crear un Google Form o un pequeño WebApp de Apps Script que reciba comentarios y los almacene en Google Sheets.
- Añadir código en `script.js` para leer los comentarios desde una API pública (Apps Script) y mostrarlos en la página.

Si quieres, te doy el código y te guío paso a paso para desplegar la solución con tu cuenta Google.
